# Repochat backend dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.83.0
uvicorn[standard]>=0.20.0

# HTTP client (used by GitHub MCP client in Phase 2)
httpx>=0.23.0

# Environment variable loading
python-dotenv>=0.21.0

# Gemini SDK (Phase 2+)
google-generativeai>=0.3.0

# Pydantic v2 (comes with FastAPI, pinned for clarity)
pydantic>=1.10.0
